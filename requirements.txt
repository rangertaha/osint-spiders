# Kept in sync with [project.dependencies] in pyproject.toml.
# Prefer `uv sync` (uses uv.lock, including the lxml>=6 override needed on
# Python 3.14); this file exists for plain-pip installs on Python 3.12/3.13.
Scrapy>=2.17,<3
scrapy-redis>=0.9.1,<1
elasticsearch>=9,<10
feedparser>=6.0.12,<7
newspaper4k[nlp]>=0.9.5,<1
pika>=1.4,<2
lxml_html_clean>=0.4
